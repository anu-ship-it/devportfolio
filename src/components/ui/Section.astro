---
interface Props {
  class?: string;
  size?: "sm" | "md" | "lg";
}

const {
  class: className = "",
  size = "md",
} = Astro.props;

const spacing = {
  sm: "py-16",
  md: "py-24",
  lg: "py-32",
};
---

<section
  class:list={[
    spacing[size],
    className,
  ]}
>
  <slot />
</section>
